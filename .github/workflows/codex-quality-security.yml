name: codex-quality-security

on:
  pull_request:
  push:
    branches: [main, master]
  schedule:
    - cron: "0 13 * * 1"

jobs:
  verify:
    name: codex_verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Linux desktop dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            pkg-config \
            libglib2.0-dev \
            libgtk-3-dev \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.29.2
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Install dependencies
        run: |
          if [ -f package.json ]; then
            pnpm install --frozen-lockfile || pnpm install
          fi
          if [ -f ui/package.json ]; then
            pnpm -C ui install --frozen-lockfile || pnpm -C ui install
          fi
      - name: Ensure cargo-audit when required
        run: |
          if grep -q "cargo audit" .codex/verify.commands; then
            cargo install cargo-audit --locked
          fi
      - name: Install Playwright browser when required
        run: |
          if grep -q "test:e2e" .codex/verify.commands; then
            pnpm exec playwright install chromium
          fi
      - name: Run canonical repo gates
        run: bash ./.codex/scripts/run_verify_commands.sh

  secrets:
    name: codex_secrets
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: TruffleHog secret scan
        uses: trufflesecurity/trufflehog@main
        with:
          extra_args: --results=verified

  sast:
    name: codex_sast
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Semgrep
        uses: returntocorp/semgrep-action@v1
        with:
          config: p/default

  dependency_and_misconfig:
    name: codex_dependency_and_misconfig
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Trivy FS scan
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: fs
          scanners: vuln,misconfig,secret
          format: table
          severity: HIGH,CRITICAL
          exit-code: "1"
          ignore-unfixed: true

  scorecard:
    name: codex_scorecard
    if: github.event_name == 'schedule'
    permissions:
      security-events: write
      id-token: write
      contents: read
      actions: read
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: OpenSSF Scorecard
        uses: ossf/scorecard-action@v2.3.1
